# OpenAI API Configuration (only needed for generating new puzzles)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview

# Note: The website does not require these keys to run.
# They are only needed if you want to generate new puzzles using scripts/generateAllPuzzles.js